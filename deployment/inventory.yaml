all:
  children:
    k8s_clusters:
      hosts:
        caktus-ratom:
        ratom-staging:
    staging:
      children:
        k8s_clusters:
#all:
#  children:
#    k8s_clusters:
#      vars:
#        ansible_connection: local
#        k8s_namespace: "ratom-{{ env_name }}"
#        k8s_container_name: web
#        k8s_container_image: govsanc/ratom-web
#        k8s_container_image_tag: latest
#        k8s_container_resources:
#          # Lower resource limits for Nginx, since it doesn't need much
#          requests:
#            memory: "10Mi"
#            cpu: "100m"
#        k8s_container_port: 80
#        k8s_container_ingress_paths:
#          - /
#        # Only deploy web container from this repo
#        k8s_templates:
#          - name: web.yaml.j2
#            state: present
#      hosts:
#        caktus-ratom:
#          env_name: staging
#          k8s_kube_context: caktus-ratom
#          k8s_domain_names:
#            - ratom-caktus-aks.caktus-built.com
